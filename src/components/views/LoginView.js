export default function name(params) {
  return (
    <div>
      <form action="">
        <input type="mail" />
        <input type="password" />
        <button type="submit">Увійти</button>
      </form>
    </div>
  );
}
